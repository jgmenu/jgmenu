SHORT TERM THINGS TO DO
=======================
Add --csv-file= and --csv-cmd= to documentation

Set width when filter applied (long items don't currently fit)

Deal with return_to_parent() when parent falls of the bottom of menu

Update examples/
    - Read TINT2_CONFIG environment variable
    - Create README
    - Make ex??.sh self-sufficient
    - Add --file= type arg to jgmenu-init.sh to sort out config files

Add atexit()-cleanup() so that it's easier to spot potential run-time memory
leaks with ASAN and Valgrind. (Also replace strdups+free with x-alternatives)

Create arg.c for command line args. At the moment jgmenurc take precedence over
cmd line args (which is wrong)

Add menu_height config option

=== Upversion to 0.6 ===

  - Add csv_cmd to config.c
  - Add 'stdin' (i.e. =use csv cmd) config option (make stdin=0 default)
  - Set stdin=1 when invoking --simple or --vsimple

  - Re-factor jgmenu_run lx/pmenu/xdg/csv, etc. by using --csv-*

  - Remove `jgmenu_run restart`

  - Add watch.c
	- Check tint2rc 'modified time' and re-read on change
	- Consider the same for jgmenurc and tint2-sessionfile (bl)

=== Upversion to 0.7 ===

VARIOUS THINGS TO DO IN NO PARTICULAR ORDER
===========================================

Add CENTER to menu_halign and menu_valign

argv_buf delim

Add explaination on JGMENU_EXEC_PATH

Remove XINERAMA ifdefs

Rename filter list (as it clashes with filter_) (maybe 'disp'?)

Add shareness with symlinks

widgets.c
---------
	enum fixed_type {ICON, RECT, TEXT, SEARCH};
	@type-action-x-y-w-h-r-halign-valign-fgcol-bgcol-content
		NOTES:
		  - x, y: distance from edge
		  - 'content' is icon_path or text
		  - For RECT, a 1px thick border will be drawn using fgcol
Add scroll_bar

Add man-page for jgmenu_run ob

Write jgmenu-init.sh --help message.

Add jgmenu-setup

Remove harded coded variables (such as 22, etc)

ISSUES
======
The window is 1px out on Ubuntu (up and left). Investigate.

When starting (not awaking) jgmenu, tint2 sets the pointer to "hour glass".
Consider how this could be avoided.
